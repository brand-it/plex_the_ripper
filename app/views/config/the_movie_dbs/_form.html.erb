<%= form_with(model: config_the_movie_db, local: true) do |form| %>
  <% if config_the_movie_db.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(config_the_movie_db.errors.count, "error") %> prohibited this config_the_movie_db from being saved:</h2>

      <ul>
        <% config_the_movie_db.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
