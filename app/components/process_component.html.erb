<div class="bg-process" id="<%= dom_id %>">
  <div class="header"><%= title %></div>
  <% if link %>
    <div class="body p-3 pb-1"><%= body %></div>
    <div class="link p-3"><%= link %></div>
  <% else %>
    <div class="body p-3"><%= body %></div>
  <% end %>
</div>
